# R for travis: see documentation at https://docs.travis-ci.com/user/languages/r

language: R
sudo: false
cache: packages

apt_packages:
 - openjdk-8-jdk

before_install:
 - R CMD javareconf 

after_success:
    - Rscript -e 'covr::codecov()'